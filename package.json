{
  "name": "prosemirror-flat-list-monorepo",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@7.27.0",
  "scripts": {
    "lint": "eslint .",
    "build": "pnpm run -r --stream build",
    "dev": "pnpm run -r --parallel dev",
    "docs": "typedoc",
    "test": "pnpm run -r --stream test run",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "ci:version": "changeset version && pnpm install --no-frozen-lockfile && pnpm build && pnpm docs",
    "ci:publish": "pnpm publish -r --no-git-checks"
  },
  "devDependencies": {
    "@antfu/ni": "^0.20.0",
    "@changesets/cli": "^2.26.0",
    "@ocavue/eslint-config": "^0.4.7",
    "eslint": "^8.34.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.4",
    "typedoc": "^0.23.25",
    "typedoc-plugin-markdown": "^3.14.0",
    "typescript": "^4.9.5"
  },
  "renovate": {
    "extends": [
      "github>ocavue/config-renovate"
    ]
  }
}
